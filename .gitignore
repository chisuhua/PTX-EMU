# 构建目录
build/
cmake-build-*/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json
Makefile

# ANTLR生成的文件
src/build/
src/antlr4_generated_src/

# VS Code
#.vscode/
.antlr/

# 库文件
lib/
bin/

# 其他临时文件
*.so*
.aider*
ptx_emu*.log

# PTX 文件（但保留 tests/ptx 目录下的）
*.ptx
!tests/ptx/*.ptx

# fix-path-coverage-gaps: Path 1B PTXIR fat-binary e2e artifacts
!tests/e2e/path_1B_ptxir_fatbinary/**/*.ptx
!tests/e2e/path_1B_ptxir_fatbinary/**/*.cubin
!tests/e2e/path_1C_driver_api/**
!tests/e2e/path_2D_image_executor/**
!tests/ptxir/baselines/*.bin
!tests/ptxir/baselines/*.md
!tests/ptxir/baselines/.gitkeep

# 缓存目录
.cache/

# Git commit review temporaries
*.bak
*.orig
*.rej
2.log
tmp_fix.txt
validation_test.txt
config.ini.bak

# Build directories
build_release/
build/

# Testing temporary files
Testing/Temporary/
tests/ptxir/*_tmp.ptxir
tests/three_mode_testing/debug_*.tmp

# Mini test config (temporary)
configs/mini_test.ini

__pycache__/
# Added by code-review-graph
.code-review-graph/

# Worktrees
.worktrees/

# Sisyphus orchestration tool state (session tracking, plans, evidence)
.omo/

# ⚠️ .code-review-graph/ 当前活跃使用（5 个 MCP server 进程运行中）
#   - 配置: .mcp.json + .opencode.json
#   - 工具: code-review-graph MCP（query_graph, get_impact_radius 等）
#   - 清理会中断 5 个活跃 agent 会话
#   - 删除前需先 kill 所有 `python3 -m code_review_graph serve` 进程
#   - 详见 .code-review-graph/README.md
.code-review-graph/
.understand-anything/
build_off/

# rddf runtime state (regenerated by guide-* workflow each session; do NOT commit)
.rddf/state/*.lock
_lib/                    # rddf-workflow dev source (not part of PTX-EMU)
_lib                     # rddf-workflow dev symlink (in case it's a symlink)
.rddf/state/sessions.json
.rddf/state/sessions.archive.json
.rddf/state/roadmap-state.json
.rddf/state/.arch-quality-report.json
.rddf/state/.deps-output.md
.rddf/state/.arch-handoff.json
.rddf/state/.plan-handoff.json
.rddf/state/.design-handoff.json
.rddf/state/.env-cache.json
