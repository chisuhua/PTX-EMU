// =============================================================================
// PTX Qualifier Type Definition — Phase 1.5b forwarding shim
// =============================================================================
// Canonical: include/ptxemu/ir/ptx_qualifier.def (ptxemu::ir namespace wrap).
// This shim exists for backward compat one release cycle (task 9.4).
// Any X(enum_value, enum_name, string_value) edit MUST go to the canonical file.
#include <ptxemu/ir/ptx_qualifier.def>
