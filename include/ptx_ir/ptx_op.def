// =============================================================================
// PTX Statement Type Definition — Phase 1.5b forwarding shim
// =============================================================================
// Canonical: include/ptxemu/ir/ptx_op.def (ptxemu::ir namespace wrap).
// This shim exists for backward compat one release cycle (task 9.4).
// Any X(enum_value, type_name, lowercase_string, op_count, struct_kind) edit MUST
// go to the canonical file.
#include <ptxemu/ir/ptx_op.def>
